# Server configuration
server.port=8000

# Application name
spring.application.name=analytics-service

# Virtual threads configuration (Java 19+)
spring.threads.virtual.enabled=true

# Logging
logging.level.com.leaderboard.analytics=INFO
logging.level.root=WARN